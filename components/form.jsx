import React from 'react';
import FormGroup from './formGroup';


const Form = props => (
    <form {...props} />
);


Form.propTypes = {
};


Form.defaultProps = {
};


Form.Group = FormGroup;


export default Form;
